import { merge } from 'ramda'

import runTest from '../../test/runTest'

const findOptions = {
  connect: {
    api_key: 'abc123',
  },
  body: {
    api_key: 'abc123',
  },
}

test('client.receivableUnits.all', () =>
  runTest(merge(findOptions, {
    method: 'GET',
    subject: client => client.receivableUnits.all(),
    url: '/receivable-units',
  }))
)

